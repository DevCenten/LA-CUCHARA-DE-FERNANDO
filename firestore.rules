rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /artifacts/{appId}/public/data/fernando_menu/{document=**} {
      allow read: true;
      allow write: if request.auth.uid != null;
    }
  }
}